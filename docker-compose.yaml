version: '3'
services:
  web:
    platform: linux/amd64
    build: 
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
  tests:
    platform: linux/amd64
    build: 
      context: .
      dockerfile: Dockerfile.dev
    command: ["yarn", "test"]